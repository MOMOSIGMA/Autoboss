self.addEventListener('install', function (e) {
  console.log('✅ Service Worker installé');
});

self.addEventListener('fetch', function (event) {
  // Tu peux personnaliser le comportement ici plus tard
});
